apiVersion: apps/v1
kind: Deployment
metadata:
  name: podrecover-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: podrecover-operator
  template:
    metadata:
      labels:
        name: podrecover-operator
    spec:
      serviceAccountName: podrecover-operator
      containers:
        - name: podrecover-operator
          # Replace this with the built image name
          image: REPLACE_IMAGE
          command:
          - podrecover-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "podrecover-operator"
